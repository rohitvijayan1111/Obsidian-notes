
- Parsing an string input to an integer(since Console.ReadLine always gets us a string value)
- exception when we give a invalid string input to convert to an integer
-

```
int.Parse(userinput);
```
